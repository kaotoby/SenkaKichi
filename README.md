# SenkaKichi
戦果基地

## To do
### First priority
- [ ] `Front``Back` Add charts to Server/Info
- [ ] `Front` Add a DateTimePicker on Home/Index to get Server/Ranking and Server/Info data in past
- [ ] `Service` Re-write the logic to update senka data. including login logic and retry logic
- [ ] `Front``Back``Service``DB` Display the ranking within all server (if less than 10000) in Server/Ranking, Player/Info

### After Online
- [ ] `Service` Post a Tweet that shows the top 3 players after each update
- [ ] `Front``Back` Add more stuff that registed can do (ex. add friend)
- [ ] `Service` Re-write user verification logic (api request interval)
