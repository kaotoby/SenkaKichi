@model short?
@if (Model.HasValue) {
    short value = Model.Value;
    if (value == 0) {
        <span class="cl-gray">(&uarr;0)</span>
    } else if (value < 0) {
        <span class="cl-red">(&uarr;@Math.Abs(value))</span>
    } else {
        <span class="cl-green">(&darr;@value)</span>
    }
}